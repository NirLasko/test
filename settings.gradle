pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = 'AlpineTrip2026'
include ':AlpineTripAndroid:app'
project(':AlpineTripAndroid:app').projectDir = file('AlpineTripAndroid/app')
